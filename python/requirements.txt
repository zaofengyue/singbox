# py-sb single-file deployment
# Zero external pip dependencies required (all modules built into Python 3.8+ stdlib).
# Added certifi to satisfy PaaS buildpacks that require at least one package in requirements.txt
certifi>=2024.2.2
